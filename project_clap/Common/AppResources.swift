import Foundation
import UIKit

struct AppResources {
    struct ColorResources {
        static let baseColor = UIColor(hex: "7289ad")
    }
    
    struct FontResources {
        static let topLabelFont = UIFont.systemFont(ofSize: 100)
    }
}
